const data = {
  // Get key & token from:
  // https://trello.com/app-key     (Must be logged in First)
  key: "YOUR-KEY",
  token: "YOUT-TOKEN",
  // Get username ID from :
  // https://api.trello.com/1/members/[username]     (attribute called "id")
  usernameID: "YOUR-ID",
};

module.exports = data;
